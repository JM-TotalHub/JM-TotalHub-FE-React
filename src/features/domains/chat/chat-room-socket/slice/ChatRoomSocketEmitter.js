import { createSlice } from '@reduxjs/toolkit';
import React from 'react';

const ChatRoomSocketEmitter = createSlice({
  name: ChatRoomSocketEmitter,
  initialState: {},
});

export default ChatRoomSocketEmitter;
